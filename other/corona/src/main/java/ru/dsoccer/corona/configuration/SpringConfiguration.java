package ru.dsoccer.corona.configuration;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SpringConfiguration {


//  @Bean
//  public Map<String, Целитель> целительMap(@Autowired List<Целитель> целительs) {
//    return целительs.stream().collect(toMap(Целитель::getType, Function.identity()));
//  }
}
