spring:
  datasource:
    url: jdbc:mysql://localhost:3306/graphql
    username: root
    password: root
    initialization-mode: always
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
